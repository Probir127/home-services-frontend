# Local development - Vite proxy handles routing to backend
# The proxy in vite.config.js forwards /api, /admin, /static, /media -> http://localhost:8000
# This variable is used in production builds only (set to your deployed backend URL)
VITE_API_URL=http://localhost:8000/api
